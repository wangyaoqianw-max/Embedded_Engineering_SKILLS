# AGENTS.md

## 基础行为

- 默认使用中文沟通和编写仓库说明，除非用户明确要求其他语言。
- 只处理用户明确要求的范围，不主动创建额外 Skill、功能或目录。
- 修改前阅读根目录 `README.md`、目标 Skill 的 `SKILL.md` 及相关测试。
- 无法验证的信息必须标为“未验证”或“待确认”，不得猜测。

## 仓库边界

- 本仓库只保存面向 Codex/Agent 的嵌入式 AI 工作流与工程决策 Skill。
- 通用嵌入式源码资产归入 `Embedded_Engineering_Library`。
- 通用构建、烧录、调试和通信工具实现归入 `Embedded_Engineering_TOOLS`。
- `skills/` 是正式 Skill 的唯一源码位置。

## Skill 结构

- 每个 Skill 使用 `skills/<skill-name>/` 独立目录，不得把 Skill 文件散放在 `skills/` 根目录。
- `SKILL.md` 是必需入口；仅在确有需要时创建 `scripts/`、`references/`、`assets/` 和 `agents/openai.yaml`。
- `SKILL.md` 保持精简；大量条件性资料放入 `references/`，确定性重复操作优先放入 `scripts/`。
- 禁止在 Skill 或仓库文档中硬编码本机绝对路径。
- 不因单个项目经验直接形成全局规则；默认策略必须服从目标项目已有架构、规范和可验证事实。

## 开发与验证

- 一个 Skill 应对应明确、可重复的工程任务或决策过程。
- 创建或修改 Skill 前先建立失败基线，确认现有行为不能稳定满足目标。
- 修改后验证显式调用、隐式触发、误触发边界和核心行为。
- 每次只开发和验收一个 Skill，完成后再进入下一个。
- `tools/` 仅保存多个 Skill 或仓库维护共同需要的辅助工具；单个 Skill 专用脚本放入该 Skill 的 `scripts/`。

## 安全与交付

- 禁止提交密钥、Token、证书私钥、个人隐私、公司内部代码或其他保密材料。
- 涉及删除、覆盖、大规模移动、外部写入、提交、推送或发布时，必须确认目标和授权范围。
- 交付前检查链接、编码、目录一致性、测试结果和 Git 差异。
- 交付时概述实际改动、验证结果和未验证项。
