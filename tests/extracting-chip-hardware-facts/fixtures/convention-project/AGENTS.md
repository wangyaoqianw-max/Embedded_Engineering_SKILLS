# Fixture project instructions

This file is at the project root. Hardware analysis reports belong in `02_Doc/Hardware_Analysis/`. Follow the existing filename style: `<chip>_<feature>_<YYYYMMDD>.md`. Do not overwrite an existing report; add a numeric suffix if needed.
